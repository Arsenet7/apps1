pipeline {
    agent { label 'master' }
    stages {
        stage('Git Clone') {
            steps {
                // Cloning the repository
                git branch: 'main', 
                    credentialsId: 'github-ssh', 
                    url: 'git@github.com:Arsenet7/apps1.git'
            }
        }
    }
}
